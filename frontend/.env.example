# Production (Vercel) — recommended: leave empty and set API_PROXY_TARGET on Vercel instead.
# The /api/* routes are proxied to Render (no CORS issues).
VITE_API_BASE_URL=

# Vercel only (Dashboard → Environment Variables):
# API_PROXY_TARGET=https://YOUR-SERVICE.onrender.com
